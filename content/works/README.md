# Works Content (MDX-Ready Notes)

The Works archive now reads from the Prisma-backed posts data in `src/lib/work-posts.ts`.

If you later want richer pages per work item, add per-work `.mdx` files here and keep IDs/slugs aligned with your data model.
